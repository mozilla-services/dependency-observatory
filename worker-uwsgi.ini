[uwsgi]
socket = /tmp/%n.sock
master = true
processes = $(PROCS)
threads = $(THREADS)
chdir = /app
enable-threads = True
mount = /=depobs.worker.wsgi:app
disable-logging = True
die-on-term = True
strict = true
single-interpreter = true
